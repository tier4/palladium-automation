# Palladium Automation Environment Configuration
# Copy this file to .env and customize for your environment

# Remote SSH Configuration
REMOTE_HOST=ga53pd01
REMOTE_USER=your_username
PROJECT_NAME=your_project_name

# Local Paths (auto-detected if not set)
# PALLADIUM_AUTOMATION_ROOT will be auto-detected as the project root
# HORNET_PATH=${PALLADIUM_AUTOMATION_ROOT}/hornet

# Bastion Configuration (optional, for SSH ProxyJump)
BASTION_HOST=10.108.64.1
BASTION_USER=${REMOTE_USER}

# Remote Working Directory (optional)
# REMOTE_WORK_DIR=/proj/${PROJECT_NAME}/work/${REMOTE_USER}

# Debug Mode (0 or 1)
DEBUG=0
